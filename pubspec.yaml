name: typedstorage
description: Typed store data based on a local file support saving data encrypted.
version: 0.0.6
homepage: https://github.com/ipconfiger/typedstorage

environment:
  sdk: '>=3.1.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  coidentity:
    git: https://gitee.com/maxleexyz/coidentity.git

flutter:
